/**
 * @readonly
 * @enum {string}
 */
const ChartTypes = Object.freeze({
    plot: 0,
    circular: 1
})

/**
 * @readonly
 * @enum {string}
 */
const CircularTypes = Object.freeze({
    pie: 0
})

/**
 * @readonly
 * @enum {string}
 */
const AnimationTypes = Object.freeze({
    mouseover: 0,
    mouseleave: 1
})