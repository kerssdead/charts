class Selectors {
    static seed = '#seed'

    static valuesCount = '#values-count'

    static chartType = '#chart-type'

    static initAnimation = '#init-animation'

    static innerRadius = '#inner-radius'

    static chart = '#chart'

    static customLabel = '#custom-label'

    static gaugeMax = '#gauge-max'

    static legend = '#legend'

    static plotType = '#plot-type'

    static xAxisType = '#x-axis-type'

    static pointsCount = '#points-count'

    static seriesWidth = '#series-width'

    static negativeValuesInPlot = '#negative-values-in-plot'

    static simple = '#simple'

    static title = '#title'

    static treePadding = '#tree-padding'

    static tooltip = '#tooltip'

    static width = '#width'

    static height = '#height'

    static baseValue = '#base-value'

    static yAxisTitle = '#y-axis-label'

    static xAxisTitle = '#x-axis-label'
}

export default Selectors